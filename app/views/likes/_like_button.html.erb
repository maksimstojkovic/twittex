<%= turbo_frame_tag "like_#{post.id}" do %>
  <% if Like.exists?(post: post, user: current_user) %>
    <%= button_to "Unlike", like_path, params: { like: { post: post } }, method: :delete %>
  <% else %>
    <%= button_to "Like", like_path, params: { like: { post: post } } %>
  <% end %>
<% end %>
