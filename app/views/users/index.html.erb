<h1>Users</h1>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to "#{user.name} (@#{user.username})", user_path(user) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No users found.</p>
<% end %>
